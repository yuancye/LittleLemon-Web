# LittleLemon-Web
(optional) if not in the manage.py directory, cd to that folder
pipenv shell #create and activate a virtual enviroment
pipenv install # install the packages in the Pipfile
python manage.py makemigrations
python manage.py migrate
python manage.py runserver
happy to explore the webpage
